#pragma once

// TODO:

/*
class StrokeManager {
public:
    void addStroke(const QVector<StrokePoint>& stroke);
    void undo();
    void clear();
    const QVector<QVector<StrokePoint>>& getStrokes() const;
    bool canUndo() const;
};
*/